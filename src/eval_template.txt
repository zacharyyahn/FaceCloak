#!/bin/bash
#SBATCH -JFaceEval
#SBATCH -N1 --gres=gpu:V100:1 --ntasks-per-node=4 --mem-per-gpu=32G
#SBATCH -n4 -t16:00:00
#SBATCH -oOUT_PATH

module load anaconda3
conda init
conda deactivate
conda activate Face

echo $CONDA_PREFIX

GALLERY_FILE="NONCLOAKED_DATASET_FILE/"
PROBE_FILE="CLOAKED_DATASET_FILE/"


python src/evaluate.py --gallery_file $GALLERY_FILE --probe_file $PROBE_FILE --num_probes 2500 --dataset_size 1.0 --cropped_im_size 112 --verbosity "none" --models MODEL_TO_EVAL --distance_function "l2"
